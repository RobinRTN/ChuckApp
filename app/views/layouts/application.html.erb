<!DOCTYPE html>
<html>
  <head>
    <title>SPORTEWDash</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, minimal-ui">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>

    <% unless (controller_name == 'bookings' && action_name == 'landing_reservation') || (controller_name == 'bookings' && action_name == 'choose_reservation') || (controller_name == 'bookings' && action_name == 'finish_reservation_exist') || (controller_name == 'bookings' && action_name == 'finish_reservation_missing')%>
      <%= render "shared/navbar" %>
    <% end %>
      <%= render "shared/flashes" %>
    <%= yield %>
  </body>
</html>
